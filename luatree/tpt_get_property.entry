NAME mixed tpt.get_property (string property, int x, int y)
DESCRIPTION Returns some property of a given particle
LINK #tpt.get_property
DETAILS

Returns some property of a given particle. The return type will be the type most
suitable for the given property.

Alternatively, you can call this function with 

    tpt.get_property (string property, int index)